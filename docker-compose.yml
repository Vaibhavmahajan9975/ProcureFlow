services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: procureflow
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - procureflow_pg:/var/lib/postgresql/data
volumes:
  procureflow_pg:
